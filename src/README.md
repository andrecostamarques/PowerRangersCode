# Source Code

This directory contains the core implementation of the project.

## Structure

- **`models/`**: Neural network architectures and the custom Selection Mask module.
- **`scripts/`**: Executable scripts for training and other operational tasks.
- **`utils/`**: Helper modules, loss functions, and training logic skeletons (e.g., `StaticMaskTraining`).
